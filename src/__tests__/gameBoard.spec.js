'use strict'
/* eslint-env jest */

/* const getCurrentPosition = require('./getCurrentPosition./public/index.js')
describe('Check position of next free tile is correct', () => {
  test('Add a letter', () => {
    const currentRow = 0
    const currentTile = 0
    const position = getCurrentPosition(currentRow, currentTile)
    expect(position.currentRow).toBe(0)
    expect(position.currentTile).toBe(1)
  })
})
*/
